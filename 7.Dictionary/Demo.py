from loguru import logger
labour_cost = {
    "Mahesh": 500,
    "Rajesh": 600
}
labour_cost["Sushil"] = 111 #adding to the dictionary

def inser_at_position(person,cost,position):
    keys =list(labour_cost.keys())
    values=list(labour_cost.value())

    keys.inser

# Correct logger.info usage with formatting
logger.info("Cost: {}", labour_cost)
